name: Docker Release

# 当推送到 release 分支时触发
on:
  push:
    branches:
      - release
      - main
    tags:
      - 'v*.*.*'  # 也支持推送版本标签触发

env:
  # 镜像名称（与 docker-compose.prod.yml 中的镜像名称保持一致）
  IMAGE_NAME: nnnnzs/react-nnnnzs-cn

jobs:
  build-and-deploy:
    name: 构建并部署 Docker 镜像
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整的 git 历史，用于版本号生成

      - name: 设置版本号
        id: version
        run: |
          # 生成版本号
          # 优先使用 git tag，如果没有则使用日期(精确到小时)
          if git describe --tags --exact-match 2>/dev/null; then
            VERSION=$(git describe --tags --exact-match)
          else
            # 格式：v{年}.{月}.{日}.{时}
            VERSION="v$(date +'%Y.%m.%d.%H')"
          fi
          
          echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
          echo "SHORT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_OUTPUT
          
          echo "📦 构建版本: ${VERSION}"

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 构建 Docker 镜像（不推送）
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.prod
          platforms: linux/amd64
          push: false
          load: true
          tags: ${{ env.IMAGE_NAME }}:latest,${{ env.IMAGE_NAME }}:${{ steps.version.outputs.VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            VERSION=${{ steps.version.outputs.VERSION }}
            BUILD_DATE=${{ steps.version.outputs.BUILD_DATE }}
            COMMIT_SHA=${{ steps.version.outputs.SHORT_SHA }}
            DATABASE_URL=${{ secrets.DATABASE_URL }}
            WX_APP_ID=${{ secrets.WX_APP_ID }}
            WX_APP_SECRET=${{ secrets.WX_APP_SECRET }}
            REDIS_HOST=${{ secrets.REDIS_HOST }}
            REDIS_PORT=${{ secrets.REDIS_PORT }}
            REDIS_DB=${{ secrets.REDIS_DB }}
            ANTHROPIC_AUTH_TOKEN=${{ secrets.ANTHROPIC_AUTH_TOKEN }}
            ANTHROPIC_BASE_URL=${{ secrets.ANTHROPIC_BASE_URL }}
            ANTHROPIC_MODEL_NAME=${{ secrets.ANTHROPIC_MODEL_NAME }}
            OPENAI_AUTH_TOKEN=${{ secrets.OPENAI_AUTH_TOKEN }}
            OPENAI_BASE_URL_PROXY=${{ secrets.OPENAI_BASE_URL_PROXY }}
            OPENAI_MODEL_NAME=${{ secrets.OPENAI_MODEL_NAME }}

      - name: 验证镜像是否存在
        run: |
          IMAGE_TAG="${{ env.IMAGE_NAME }}:latest"
          echo "🔍 检查镜像: ${IMAGE_TAG}"
          docker images | grep -E "${{ env.IMAGE_NAME }}.*latest" || docker images
          if ! docker image inspect "${IMAGE_TAG}" > /dev/null 2>&1; then
            echo "❌ 镜像不存在: ${IMAGE_TAG}"
            echo "可用镜像列表:"
            docker images
            exit 1
          fi
          echo "✅ 镜像存在: ${IMAGE_TAG}"

      - name: 导出 Docker 镜像
        run: |
          IMAGE_TAG="${{ env.IMAGE_NAME }}:latest"
          IMAGE_FILE="react-nnnnzs-cn-${{ steps.version.outputs.VERSION }}.tar"
          
          echo "📦 导出镜像: ${IMAGE_TAG} -> ${IMAGE_FILE}"
          docker save -o "${IMAGE_FILE}" "${IMAGE_TAG}"
          
          if [ ! -f "${IMAGE_FILE}" ]; then
            echo "❌ 镜像导出失败: ${IMAGE_FILE} 不存在"
            exit 1
          fi
          
          echo "IMAGE_FILE=${IMAGE_FILE}" >> $GITHUB_ENV
          
          # 显示文件大小
          ls -lh "${IMAGE_FILE}"
          echo "✅ 镜像导出成功: ${IMAGE_FILE}"

      - name: 配置 SSH 密钥
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          DEPLOY_PORT="${{ secrets.DEPLOY_PORT }}"
          DEPLOY_PORT=${DEPLOY_PORT:-22}
          ssh-keyscan -p ${DEPLOY_PORT} ${{ secrets.DEPLOY_HOST }} >> ~/.ssh/known_hosts

      - name: 传输镜像到服务器
        run: |
          echo "🚀 传输镜像到服务器..."
          DEPLOY_PORT="${{ secrets.DEPLOY_PORT }}"
          DEPLOY_PORT=${DEPLOY_PORT:-22}
          scp -i ~/.ssh/deploy_key \
              -P ${DEPLOY_PORT} \
              "${IMAGE_FILE}" \
              ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}:${{ secrets.DEPLOY_DIR }}/"${IMAGE_FILE}"
          echo "✅ 镜像传输完成"

      - name: 在服务器上加载镜像并部署
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT }}
          script: |
            cd ${{ secrets.DEPLOY_DIR }}
            
            IMAGE_FILE="react-nnnnzs-cn-${{ steps.version.outputs.VERSION }}.tar"
            IMAGE_TAG="nnnnzs/react-nnnnzs-cn:latest"
            
            echo "📥 加载镜像: ${IMAGE_FILE}"
            docker load -i "${IMAGE_FILE}"
            
            echo "🗑️  删除镜像文件: ${IMAGE_FILE}"
            rm -f "${IMAGE_FILE}"
            
            echo "🔄 执行部署..."
            # 执行部署脚本
            if [ -f "${{ secrets.DEPLOY_SCRIPT }}" ]; then
              bash "${{ secrets.DEPLOY_SCRIPT }}" deploy
            else
              ${{ secrets.DEPLOY_SCRIPT }} deploy
            fi
            
            echo "✅ 部署完成！"

      - name: 清理本地镜像文件
        if: always()
        run: |
          if [ -f "${IMAGE_FILE}" ]; then
            echo "🗑️  清理本地镜像文件..."
            rm -f "${IMAGE_FILE}"
          fi

      - name: 生成构建摘要
        run: |
          echo "## 🐳 Docker 镜像构建并部署成功！" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 镜像信息" >> $GITHUB_STEP_SUMMARY
          echo "- **版本号**: \`${{ steps.version.outputs.VERSION }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **镜像名称**: \`${{ env.IMAGE_NAME }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit SHA**: \`${{ steps.version.outputs.SHORT_SHA }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **构建时间**: \`${{ steps.version.outputs.BUILD_DATE }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🚀 部署方式" >> $GITHUB_STEP_SUMMARY
          echo "镜像已通过 scp 传输到服务器并加载完成。" >> $GITHUB_STEP_SUMMARY

      - name: 发送部署成功通知
        if: success()
        run: |
          curl -H "Content-Type:application/json" -X POST -d '{"title":"自动部署","content":"react.nnnnzs.cn部署完成","url":"https://www.nnnnzs.cn"}' "${{ secrets.DEPLOY_NOTIFY_URL }}"

      - name: 发送部署失败通知
        if: failure()
        run: |
          curl -H "Content-Type:application/json" -X POST -d '{"title":"自动部署","content":"react.nnnnzs.cn部署失败","url":"https://www.nnnnzs.cn"}' "${{ secrets.DEPLOY_NOTIFY_URL }}"

      - name: 构建失败通知
        if: failure()
        run: |
          echo "## ❌ Docker 镜像构建失败！" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "请检查构建日志以获取详细错误信息。" >> $GITHUB_STEP_SUMMARY
