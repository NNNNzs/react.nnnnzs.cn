name: Docker Release

# 当推送到 release 分支时触发
on:
  push:
    branches:
      - release
      - main
    tags:
      - 'v*.*.*'  # 也支持推送版本标签触发

env:
  # 阿里云 ACR 相关配置（需要在 GitHub Secrets 中设置）
  ACR_REGISTRY: ccr.ccs.tencentyun.com
  ACR_USERNAME: ${{ secrets.ACR_USERNAME }}
  ACR_PASSWORD: ${{ secrets.ACR_PASSWORD }}
  # 镜像名称（格式：registry/namespace/repo）
  IMAGE_NAME: ccr.ccs.tencentyun.com/nnnnzs/react.nnnnzs.cn

jobs:
  build-and-push:
    name: 构建并推送 Docker 镜像
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整的 git 历史，用于版本号生成

      - name: 设置版本号
        id: version
        run: |
          # 生成版本号
          # 优先使用 git tag，如果没有则使用日期(精确到小时)
          if git describe --tags --exact-match 2>/dev/null; then
            VERSION=$(git describe --tags --exact-match)
          else
            # 格式：v{年}.{月}.{日}.{时}
            VERSION="v$(date +'%Y.%m.%d.%H')"
          fi
          
          echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
          echo "SHORT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_OUTPUT
          
          echo "📦 构建版本: ${VERSION}"

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录阿里云 ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.ACR_REGISTRY }}
          username: ${{ env.ACR_USERNAME }}
          password: ${{ env.ACR_PASSWORD }}

      - name: 提取 Docker 元数据
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            # 只标记为 latest
            type=raw,value=latest

      - name: 构建并推送 Docker 镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.prod
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache
          build-args: |
            VERSION=${{ steps.version.outputs.VERSION }}
            BUILD_DATE=${{ steps.version.outputs.BUILD_DATE }}
            COMMIT_SHA=${{ steps.version.outputs.SHORT_SHA }}
            DATABASE_URL=${{ secrets.DATABASE_URL }}
            WX_APP_ID=${{ secrets.WX_APP_ID }}
            WX_APP_SECRET=${{ secrets.WX_APP_SECRET }}
            REDIS_HOST=${{ secrets.REDIS_HOST }}
            REDIS_PORT=${{ secrets.REDIS_PORT }}
            REDIS_DB=${{ secrets.REDIS_DB }}
            ANTHROPIC_AUTH_TOKEN=${{ secrets.ANTHROPIC_AUTH_TOKEN }}
            ANTHROPIC_BASE_URL=${{ secrets.ANTHROPIC_BASE_URL }}
            ANTHROPIC_MODEL_NAME=${{ secrets.ANTHROPIC_MODEL_NAME }}
            OPENAI_AUTH_TOKEN=${{ secrets.OPENAI_AUTH_TOKEN }}
            OPENAI_BASE_URL_PROXY=${{ secrets.OPENAI_BASE_URL_PROXY }}
            OPENAI_MODEL_NAME=${{ secrets.OPENAI_MODEL_NAME }}

      - name: 生成构建摘要
        run: |
          echo "## 🐳 Docker 镜像构建成功！" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 镜像信息" >> $GITHUB_STEP_SUMMARY
          echo "- **版本号**: \`${{ steps.version.outputs.VERSION }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **镜像名称**: \`${{ env.IMAGE_NAME }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit SHA**: \`${{ steps.version.outputs.SHORT_SHA }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **构建时间**: \`${{ steps.version.outputs.BUILD_DATE }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🚀 使用方法" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "# 登录阿里云 ACR" >> $GITHUB_STEP_SUMMARY
          echo "docker login --username=\$ACR_USERNAME ${{ env.ACR_REGISTRY }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# 拉取最新镜像" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ env.IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: 推送失败通知
        if: failure()
        run: |
          echo "## ❌ Docker 镜像构建失败！" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "请检查构建日志以获取详细错误信息。" >> $GITHUB_STEP_SUMMARY

  deploy:
    name: 部署到服务器
    runs-on: ubuntu-latest
    needs: build-and-push  # 等待构建完成
    # 只在构建成功时执行部署
    if: success()
    
    steps:
      - name: SSH 连接到服务器并执行部署脚本
        id: deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          script: |
            # 切换到部署目录
            cd ${{ secrets.DEPLOY_DIR }}
            
            # 执行指定的部署脚本
            if [ -f "${{ secrets.DEPLOY_SCRIPT }}" ]; then
              bash "${{ secrets.DEPLOY_SCRIPT }}"
            else
              ${{ secrets.DEPLOY_SCRIPT }}
            fi
            
            echo "✅ 部署完成！"

      - name: 发送部署成功通知
        if: success()
        run: |
          curl -H "Content-Type:application/json" -X POST -d '{"title":"自动部署","content":"react.nnnnzs.cn部署完成","url":"https://react.nnnnzs.cn"}' "${{ secrets.DEPLOY_NOTIFY_URL }}"

      - name: 发送部署失败通知
        if: failure()
        run: |
          curl -H "Content-Type:application/json" -X POST -d '{"title":"自动部署","content":"react.nnnnzs.cn部署失败","url":"https://react.nnnnzs.cn"}' "${{ secrets.DEPLOY_NOTIFY_URL }}"
