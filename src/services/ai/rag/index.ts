/**
 * @deprecated Chat Agent 主实现已迁移到 `src/services/ai/chat-agent`。
 * 这里仅保留兼容导出；新代码请直接从 `@/services/ai/chat-agent` 导入。
 */

export { chatAgentStream, chatRAGAgentStream } from '@/services/ai/chat-agent';
export type { ChatAgentParams, RAGAgentParams } from '@/services/ai/chat-agent';
