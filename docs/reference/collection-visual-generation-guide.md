# 合集视觉资源生成指南

## 用途

本文档用于新增或重做博客合集的日间/夜间封面图、竖长封面视频和页面背景图。提示词不绑定具体模型，可用于常见图片生成、图片编辑和图生视频工具。

运行时字段：

| 资源 | 字段 | 比例 | 页面用途 |
|---|---|---:|---|
| 日间/夜间竖长封面图 | `coverImageUrl` | 9:16 | 合集列表缩略图、选中态、详情页、Open Graph |
| 日间/夜间竖长封面视频 | `coverVideoUrl` | 9:16 | 首页选中态和详情页竖长封面槽 |
| 日间/夜间页面背景图 | `backgroundImageUrl` | 16:9 | 合集详情页首屏静态空间背景 |
| 合集首页背景图 | `TbConfig: collections.home.background.day/night` | 16:9 | `/collections` 整页环境背景 |

不生成或配置独立视频 Poster，不使用 16:9 背景视频。

## 统一视觉约束

### 品牌语气

- 关键词：长期写作、个人知识档案、克制的技术感、真实使用痕迹、可持续积累。
- 日间：自然光、低饱和材质、清晰但不过曝、偏编辑设计与实体档案。
- 夜间：蓝黑环境、冷青细节光、克制的数字界面感，避免廉价霓虹和彩色灯带堆叠。
- 同一合集的昼夜资源必须保留相同主体、镜头、构图和视觉锚点，只改变时间、光线和材质反馈。

### 禁止项

- 不生成任何文字、字母、数字、Logo、水印、二维码或伪 UI。
- 不使用知名品牌、影视、游戏角色或可识别公众人物。
- 不把手机、书脊、海报标题当作主要信息载体。
- 不使用夸张景深、鱼眼、倾斜地平线、过度辉光或高频粒子。
- 不让关键主体贴近边缘；上下各保留约 8% 安全区，左右各保留约 10% 安全区。

## 竖长封面图

### 输出规范

- 比例：严格 9:16。
- 建议尺寸：`1440 × 2560`；最低 `900 × 1600`。
- 单一明确主体，允许有纵向空间层次，但不能像手机界面或细长尺子。
- 中央 60% 区域保留稳定视觉锚点，适合 `object-fit: cover` 下轻微裁切。
- 图片本身不包含合集标题，标题由网页语义化文本展示。

### 日间封面通用提示词

```text
为个人技术博客的文章合集创作一张高级、克制、可长期使用的竖版主题封面。

合集名称：{合集名称}
合集描述：{合集描述}
内容关键词：{3-6 个关键词}
核心视觉隐喻：{与主题有关的真实空间、器物、材料或自然景观}

严格 9:16 竖版构图，单一明确主体，具有前景、中景和背景层次，主体位于中央安全区。自然日光，低饱和但不灰暗，材质真实，细节精致，像高质量编辑摄影与实体档案设计的结合。画面安静、有长期积累感，不做宣传海报，不出现人物特写。为网页叠加标题保留稳定的低细节区域。

不要生成任何文字、字母、数字、Logo、水印、二维码、边框、手机界面、书名或伪 UI。不要过度景深、过曝、高饱和、夸张透视或密集装饰。
```

### 夜间封面编辑提示词

夜间版本应使用已确认的日间封面作为唯一图像输入，不重新生成构图。

```text
把输入的日间竖版封面转换为同一地点、同一镜头、同一主体和同一构图的夜间版本。

严格保留所有物体的位置、比例、轮廓、相机高度和焦距，不新增或删除主体。把环境改为深蓝黑夜色，加入克制的冷青反射光与少量局部指示光；暗部仍保留材质纹理和层次，不压成纯黑。整体安静、精确、具有档案系统般的技术感，但不是霓虹广告。

保持严格 9:16。不要生成文字、Logo、人物、伪 UI、额外屏幕、彩色灯带、强烈洋红色、过量辉光或雨幕遮挡。
```

## 竖长封面视频

### 输出规范

- 比例：严格 9:16，与对应主题封面图完全一致。
- 时长：6–10 秒。
- 编码：优先 MP4/H.264，可补 WebM；网页不使用音轨。
- 播放：首尾可无缝循环，固定镜头，无推拉摇移，无变焦。
- 动态幅度：低；只让画面中已有元素轻微运动。
- 不扩展成横版，不改变主体位置，不重新设计场景。

### 图生视频通用提示词

```text
以输入的 9:16 竖版合集封面为唯一视觉基准，生成 8 秒无缝循环视频。

保持原图的镜头、构图、主体位置、色彩关系、材质和纵横比完全不变。相机绝对固定，不推近、不拉远、不平移、不旋转。只让原画中合理的局部元素发生低幅度运动，例如自然光影缓慢移动、树叶轻摆、雨滴沿玻璃滑落、指示灯轻微呼吸、屏幕反光缓慢变化、空气微粒极少量漂浮。第一帧与最后一帧在光线、物体位置和运动相位上自然衔接。

严格 9:16，6-10 秒，无声。不要新增物体、人物、文字、Logo、伪 UI、镜头运动、景深抽动、画面闪烁、结构形变、液化、快速粒子或大范围环境变化。
```

### 日间动态建议

- 光斑缓慢移动。
- 纸张、窗帘、叶片有极轻微摆动。
- 局部反射或设备指示灯缓慢变化。
- 避免明显昼夜变化和大范围云层延时。

### 夜间动态建议

- 雨滴、冷色反光或局部指示灯轻微运动。
- 远处光源缓慢呼吸，不能频闪。
- 暗部保持稳定，避免自动曝光来回变化。

## 已配置合集的视频生成提示词（2026-07-29）

以下提示词面向不支持独立负面提示词的图生视频平台；每段均已内置限制。上传对应主题的竖长封面作为唯一参考图，选择图生视频模式。统一参数为：严格 `9:16`、`8 秒`、静音、固定镜头、首尾无缝循环。**全屋智能之路**的视频已完成，本轮整套日间/夜间方案均跳过。

| 合集 | 日间竖长封面 | 夜间竖长封面 |
|---|---|---|
| 大模型学习 | [打开日间图](https://static.nnnnzs.cn/upload/image-gen/33d6cbf6-a4d9-411a-aa00-a4168dc0e669.png) | [打开夜间图](https://static.nnnnzs.cn/upload/image-gen/1a939782-4918-4245-92e4-3c0c5590e834.png) |
| 小破站建设 | [打开日间图](https://static.nnnnzs.cn/upload/image-gen/7eadb264-4bda-4ef4-9178-da85e361c873.png) | [打开夜间图](https://static.nnnnzs.cn/upload/image-gen/18209fd9-8f9d-4d99-a5b9-63ca4e7b027c.png) |
| 前端开发 | [打开日间图](https://static.nnnnzs.cn/upload/image-gen/24f0c75c-fb3f-46cf-8a59-f8837e10cb0b.png) | [打开夜间图](https://static.nnnnzs.cn/upload/image-gen/aee637ea-b4f6-4ad0-902e-dc1dd94dacee.png) |
| 算法题解 | [打开日间图](https://static.nnnnzs.cn/upload/image-gen/fdd1e304-b5e4-42db-9604-e31a5260e7d3.png) | [打开夜间图](https://static.nnnnzs.cn/upload/image-gen/ad82a26f-cd35-43cf-affb-9d11699a736d.png) |
| 工具开发 | [打开日间图](https://static.nnnnzs.cn/upload/image-gen/fd93f007-ee6e-4795-943d-abe77b530f4c.png) | [打开夜间图](https://static.nnnnzs.cn/upload/image-gen/23540328-e820-47dc-bd00-b37e3db6d1ba.png) |
| 运维实践 | [打开日间图](https://static.nnnnzs.cn/upload/image-gen/345afe17-1773-4977-bdf5-837420d680ca.png) | [打开夜间图](https://static.nnnnzs.cn/upload/image-gen/2ad50b8a-60ed-4c87-a2a0-6235099ac381.png) |
| 生活感悟 | [打开日间图](https://static.nnnnzs.cn/upload/image-gen/385fad4f-9c1b-42dc-a7b2-6fc6be0b2b0c.png) | [打开夜间图](https://static.nnnnzs.cn/upload/image-gen/16e8a215-f6c0-42e9-8c8c-739b5a7c926e.png) |
| 旅行游记 | [打开日间图](https://static.nnnnzs.cn/upload/image-gen/06d70529-323b-4bee-a4a4-e6b5465e868a.png) | [打开夜间图](https://static.nnnnzs.cn/upload/image-gen/ba240a11-0e9c-422d-a8ff-08e51dccb699.png) |

### 大模型学习

日间：

```text
以输入的日间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。镜头绝对固定，保持桌面、书本、中央透明棱柱、四块信息板、细线和琥珀色节点的位置与比例不变。只有琥珀色小节点沿既有连接线极缓慢地依次亮起，透明棱柱上掠过非常柔和的日光反射，窗边枝叶投影轻微摆动。第 1 帧与最后一帧的节点位置和亮度完全一致。严格保持 9:16；不新增物体、文字、Logo、人物或屏幕，不推拉摇移，不闪烁、不形变。
```

夜间：

```text
以输入的夜间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。镜头绝对固定，所有书本、透明棱柱、信息板、细线和节点保持原位。仅让冷青色微光沿已有连线缓慢流动，棱柱边缘有克制的蓝色反光呼吸，暗处材质稳定可见。第 1 帧与最后一帧的光线相位、亮度和全部物体完全一致。严格保持 9:16；不新增文字、Logo、人物、伪 UI 或霓虹灯带，不推拉摇移、不闪烁、不形变。
```

### 小破站建设

日间：

```text
以输入的日间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。固定镜头，微缩房屋、服务器机柜、起重机、传送带、积木和桌面布局完全不变。只让传送带上的一个微型模块以极短距离平滑往复、服务器指示灯温和呼吸、窗外日光和细小阴影缓慢变化；结束时模块准确回到起点。严格保持 9:16；不改变建筑结构，不新增物体、文字、Logo、人物或车辆，不推拉摇移、不闪烁、不形变。
```

夜间：

```text
以输入的夜间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。镜头完全固定，微缩房屋、机柜、起重机、传送带和所有积木保持原位。仅让服务器上的少量冷青指示灯依次柔和呼吸，传送带边缘出现极弱蓝色反光，远处窗光有稳定轻微变化；所有光效在结束时回到初始状态。严格保持 9:16；不新增屏幕、文字、Logo、人物、雨幕或强霓虹，不推拉摇移、不闪烁、不形变。
```

### 前端开发

日间：

```text
以输入的日间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。镜头绝对固定，悬浮的透明界面层、环绕轨道、小球、纸张和桌面位置完全不变。只有轨道上的小球以极慢速度完成一圈平滑循环，透明层表面有细微日光反射，纸张边缘几乎不可察觉地轻动；结尾精确回到第一帧。严格保持 9:16；不出现真实文字、代码、Logo、人物或新 UI，不推拉摇移、不闪烁、不液化。
```

夜间：

```text
以输入的夜间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。固定镜头，所有透明界面层、轨道、小球和纸张维持原有构图。仅让小球沿轨道缓慢循环，界面边缘出现低强度冷青高光的呼吸变化，暗部稳定保留层次；最后一帧完全回到起始光效和小球位置。严格保持 9:16；不新增字符、代码、Logo、人物、屏幕或彩色灯带，不推拉摇移、不闪烁、不形变。
```

### 算法题解

日间：

```text
以输入的日间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。镜头完全固定，纸质起伏地形、串珠路径、圆盘和桌面器物位置不变。只让红蓝白色珠子沿既有细线极慢地推进并在结尾回到初始位置，纸面上的柔光与植物影缓慢移动。严格保持 9:16；不新增数字、公式、文字、Logo、人物或新路径，不推拉摇移、不闪烁、不形变。
```

夜间：

```text
以输入的夜间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。固定镜头，纸质地形、全部串珠、圆盘和路径保持原位。仅让少量节点由暗到亮进行克制冷青呼吸，珠子沿既有线路做极短距离可逆移动，暗蓝反光缓慢掠过纸张纹理；最终回到第一帧。严格保持 9:16；不出现公式、字符、Logo、人物或伪 UI，不推拉摇移、不频闪、不形变。
```

### 工具开发

日间：

```text
以输入的日间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。镜头绝对固定，纸张压印机、金属滚筒、齿轮、工具和书本位置完全不变。只让两只齿轮以极慢速度同步转动一小段后平滑反向，金属表面出现轻微日光流动反射，纸张边缘轻轻起伏；结尾回到完全相同的起始状态。严格保持 9:16；不新增文字、印刷内容、Logo、人物或机械部件，不推拉摇移、不闪烁、不形变。
```

夜间：

```text
以输入的夜间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。固定镜头，压印机、滚筒、齿轮、工具和书本全部保持原位。仅让齿轮进行很小幅度的可逆转动，金属边缘有低亮度冷青反射，机械接缝处的微弱指示光平缓呼吸；结束时完全回到第一帧。严格保持 9:16；不新增文字、Logo、人物、火花、屏幕或夸张霓虹，不推拉摇移、不频闪、不形变。
```

### 运维实践

日间：

```text
以输入的日间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。镜头完全固定，服务器机柜、机械臂、输送线、三只储物箱和线轴的位置完全不变。仅让机械臂末端在原地做极小幅度的平滑上下校准动作，机柜状态灯低亮度呼吸，窗边光影缓慢移动；结尾精确回到初始姿态。严格保持 9:16；不新增人物、文字、Logo、屏幕、烟雾或新设备，不推拉摇移、不闪烁、不形变。
```

夜间：

```text
以输入的夜间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。固定镜头，机柜、机械臂、输送线、储物箱和线轴维持原位。只让少量机柜状态灯按缓慢节奏依次呼吸，机械臂保持静止或仅做极短可逆的校准移动，冷青反光在金属边缘缓慢变化；最后一帧完全一致。严格保持 9:16；不新增文字、Logo、人物、警报灯、火花或赛博霓虹，不推拉摇移、不闪烁、不形变。
```

### 生活感悟

日间：

```text
以输入的日间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。镜头绝对固定，窗边书桌、山谷纸艺景观、瀑布、弯曲小路、花瓶和纸张的位置完全不变。仅让远处薄雾极慢流动，瀑布有细腻连续水流，叶片轻摆，日光缓慢掠过山谷；所有动态在结尾无缝回到初始状态。严格保持 9:16；不新增人物、文字、Logo、车辆、鸟群或新景物，不推拉摇移、不闪烁、不形变。
```

夜间：

```text
以输入的夜间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。固定镜头，山谷、瀑布、小路、桌面和花瓶保持完全原位。只让瀑布带有柔和冷蓝月光反射，山间薄雾极缓慢流动，极少量远处暖点稳定呼吸；暗部仍保留纸艺和岩石纹理，最后一帧回到第一帧。严格保持 9:16；不新增星空、人物、文字、Logo、车辆或强光源，不推拉摇移、不频闪、不形变。
```

### 旅行游记

日间：

```text
以输入的日间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。镜头绝对固定，海岸山崖、瀑布、盘山路、海面、地图纸和桌面物件位置完全不变。仅让海面产生非常轻柔的循环波纹，瀑布持续细腻流动，沿山雾气缓慢漂移，阳光在水面微弱闪烁；结束时水纹和雾气回到初始相位。严格保持 9:16；不新增人物、车辆、船只、鸟群、文字或 Logo，不推拉摇移、不闪烁、不形变。
```

夜间：

```text
以输入的夜间 9:16 封面为唯一基准，生成 8 秒无缝循环视频。固定镜头，山崖、盘山路、海面、瀑布、地图纸和桌面物件全部保持原位。仅让海面在冷蓝月光中出现细微循环波纹，瀑布有克制的青蓝反光，薄雾轻缓移动，远处路边极少量暖光稳定呼吸；最后一帧完全回到起始状态。严格保持 9:16；不新增人物、车辆、船只、文字、Logo、星轨或强霓虹，不推拉摇移、不频闪、不形变。
```

## 已生成的通用页面背景图（2026-07-29）

这两张图服务于所有合集详情页，不绑定某一合集；不自动写入数据库，待人工上传 CDN 后手动配置。两图均为严格 `1920 × 1080`，夜间图由日间图编辑生成，构图和边缘元素一致。

| 主题 | 本地资源 | 预览 | 生成提示词 |
|---|---|---|---|
| 日间 | `public/images/collections/collection-page-background-day.png` | ![日间页面背景](../../public/images/collections/collection-page-background-day.png) | 暖白哑光墙面与浅木格栅，斜射晨光和模糊植物影；左右下缘仅有少量纸艺、建筑微缩与蓝灰/暖橙细节，中央及下方中央保持低对比、低细节的 UI 安全区；高级编辑摄影与触感纸艺结合，无文字、Logo、人物、UI 或单一合集专属主体。 |
| 夜间 | `public/images/collections/collection-page-background-night.png` | ![夜间页面背景](../../public/images/collections/collection-page-background-night.png) | 以日间图为唯一编辑输入，保留构图、物件位置、裁切和安全区；只转换为深蓝黑夜景、克制冷青掠射反光与微弱紫色氛围光，保留墙面、木纹和纸材的暗部层次；无文字、Logo、人物、UI、强霓虹或新增主体。 |

上传后分别填写到各合集 `extends_json.presentation.day.backgroundImageUrl` 与 `extends_json.presentation.night.backgroundImageUrl`；如果只希望替换全站合集首页环境背景，则填写 `TbConfig` 的 `collections.home.background.day` 与 `collections.home.background.night`。

## 页面背景图

### 输出规范

- 比例：严格 16:9，建议 `1920 × 1080` 或更高。
- 页面背景是空间环境，不是视觉主体。
- 中央及主要内容区域保持低细节、低对比，边缘可以提供材质和光线线索。
- 合集首页背景必须能同时承载左侧合集轮播、中央封面和右侧资料面板。
- 单个合集详情背景可以体现主题，但不能抢过竖长封面。

### 日间页面背景提示词

```text
为个人博客的合集页面生成一张 16:9 日间空间背景。

主题：{合集主题或“多主题知识档案”}
空间隐喻：{工作室、档案室、书房、实验台、自然观察空间等}

自然柔和日光，低饱和暖中性色，真实材质，克制而高级。画面四周提供少量空间和材质细节，中央与主要内容区域保持低细节、低对比和稳定亮度，能够叠加半透明中文界面、竖长封面和文章资料。背景只负责氛围，不出现抢眼主体。

严格 16:9。不要生成文字、书名、Logo、人物、UI 面板、手机、强透视、杂乱桌面或高对比焦点。
```

### 夜间页面背景编辑提示词

```text
把输入的日间 16:9 页面背景转换为完全相同构图的夜间版本。

保留相机、空间结构、物体位置和中央低细节安全区。改为深蓝黑环境光，加入少量冷青材质反射和远处微弱灯光；暗部有层次但不纯黑，确保上方中文界面仍清晰可读。夜间版本只改变光线和时间，不重新设计空间。

不要生成文字、Logo、人物、伪 UI、强霓虹、洋红色主光、频闪灯、密集雨线或新的视觉主体。
```

## 生成顺序

1. 根据合集名称、描述和代表文章提取 3–6 个内容关键词。
2. 先确认日间竖长封面，构图不合格时不要继续生产夜间和视频。
3. 以日间封面作为唯一输入生成夜间封面。
4. 分别以日间、夜间封面生成对应 9:16 循环视频。
5. 需要时生成单个合集的 16:9 页面背景；首页背景单独生成。
6. 人工质检后上传 CDN，再填写后台日间/夜间视觉字段。

## 质检清单

- [ ] 图片和视频均符合目标比例，没有被工具自动裁成近似比例。
- [ ] 昼夜版本的主体、构图和焦点位置一致。
- [ ] 没有文字、Logo、水印、人物特写或第三方 IP。
- [ ] 9:16 封面在桌面与移动端 `object-fit: cover` 下主体仍完整。
- [ ] 视频固定镜头、低运动幅度、无闪烁，首尾循环无明显跳变。
- [ ] 视频没有音轨依赖，静音播放仍表达完整。
- [ ] 16:9 背景中央低细节，叠加页面后文字对比度合格。
- [ ] 资源使用稳定的 `https://static.nnnnzs.cn/...` CDN URL。
- [ ] 日间、夜间分别在前台首页和详情页实际切换验证。
