# 项目使用指南

## ✅ 已完成的功能

### 1. 博客前端展示
- ✅ **首页**（`/`）
  - 文章列表展示
  - 卡片式布局
  - 分页加载
  - 点击卡片查看详情
  
- ✅ **文章详情页**（`/post/[id]`）
  - Markdown 内容渲染
  - 代码高亮显示
  - 文章元信息（日期、访问量、喜欢数）
  - 标签展示
  - 点赞功能
  
- ✅ **标签页**（`/tags`）
  - 所有标签列表
  - 卡片式布局
  - 显示每个标签的文章数量
  - 点击标签查看相关文章
  
- ✅ **标签筛选页**（`/tags/[tag]`）
  - 根据标签筛选文章
  - 显示该标签下的所有文章

### 2. 用户认证
- ✅ **登录页**（`/login`）
  - 用户登录
  - 用户注册
  - 表单验证
  - 自动跳转
  
- ✅ **认证管理**
  - JWT Token 管理
  - Cookie 存储
  - 自动刷新用户信息
  - 登出功能

### 3. 后台管理
- ✅ **文章管理页**（`/admin`）
  - 文章列表展示
  - 搜索功能
  - 状态筛选（显示/隐藏）
  - 删除文章
  - 跳转到编辑页
  
- ✅ **创建文章页**（`/admin/create`）
  - 标题、描述、标签输入
  - Markdown 内容编辑
  - 封面图片设置
  - 状态设置（显示/隐藏）
  
- ✅ **编辑文章页**（`/admin/edit/[id]`）
  - 加载现有文章数据
  - 修改文章内容
  - 保存修改

### 4. API 接口
#### 博客相关
- ✅ `GET /api/post/list` - 获取文章列表（支持分页、搜索、筛选）
- ✅ `GET /api/post/[id]` - 获取文章详情
- ✅ `POST /api/post/create` - 创建文章（需要登录）
- ✅ `PUT /api/post/[id]` - 更新文章（需要登录）
- ✅ `DELETE /api/post/[id]` - 删除文章（需要登录）
- ✅ `GET /api/post/tags` - 获取所有标签
- ✅ `GET /api/post/tags/[tag]` - 根据标签获取文章
- ✅ `PUT /api/post/fav` - 更新文章统计

#### 用户认证
- ✅ `POST /api/user/login` - 用户登录
- ✅ `POST /api/user/register` - 用户注册
- ✅ `GET /api/user/info` - 获取用户信息（需要登录）
- ✅ `POST /api/user/logout` - 退出登录

### 5. 通用组件
- ✅ **Header** - 顶部导航栏，包含用户信息和菜单
- ✅ **Footer** - 页脚，包含链接和版权信息
- ✅ **PostCard** - 文章卡片组件
- ✅ **Banner** - 横幅组件

## 🚀 快速开始

### 1. 安装依赖

```bash
cd /Users/nnnnzs/project/react.nnnnzs.cn
pnpm install
```

### 2. 启动开发服务器

```bash
pnpm dev
```

访问 http://localhost:3000

### 3. 使用测试账号登录

- **账号**: admin
- **密码**: admin123

## 📱 功能演示

### 前台功能
1. 访问首页查看文章列表
2. 点击文章卡片查看详情
3. 在详情页点赞
4. 访问 `/tags` 查看所有标签
5. 点击标签查看该标签下的文章

### 后台管理
1. 访问 `/login` 登录
2. 登录后点击右上角头像，选择"管理后台"
3. 在管理后台可以：
   - 查看所有文章
   - 搜索文章
   - 创建新文章
   - 编辑现有文章
   - 删除文章

## 🎨 技术特点

### 1. Next.js 15 App Router
- 使用最新的 App Router
- 服务端和客户端组件分离
- API Routes 实现后端接口

### 2. React 19
- 使用最新的 React 19
- Hooks 管理状态
- Context 管理全局状态

### 3. Ant Design
- 使用 Ant Design 6.0 Alpha
- 响应式设计
- 中文语言包

### 4. Tailwind CSS 4
- 使用最新的 Tailwind CSS 4
- 实用优先的 CSS 框架
- 深色模式支持

### 5. TypeScript
- 完整的类型定义
- 类型安全
- 更好的开发体验

## 🔧 自定义配置

### 修改默认数据
编辑 `src/lib/db.ts` 文件中的 `mockPosts` 和 `mockUsers` 数组。

### 连接真实数据库
参考 `api.nnnnzs.cn` 项目，使用 TypeORM 连接 MySQL 数据库：

1. 安装 TypeORM 和数据库驱动
```bash
pnpm add typeorm mysql2
```

2. 创建实体文件（参考 `api.nnnnzs.cn/entities`）
3. 修改 `src/lib/db.ts` 中的数据库操作
4. 配置数据库连接

### 修改样式
- 全局样式：`src/app/globals.css`
- Tailwind 配置：`tailwind.config.js`
- 组件样式：使用 Tailwind 类名

## 📊 数据结构

### Post（文章）
```typescript
interface Post {
  id: string | number;
  title: string;           // 标题
  path: string;           // 路径
  cover: string;          // 封面图
  tags: string;           // 标签（逗号分隔）
  date: string | Date;    // 创建时间
  updated: string | Date; // 更新时间
  description: string;    // 描述
  content: string;        // Markdown内容
  hide: string;          // 是否隐藏（'0'显示，'1'隐藏）
  likes: number;         // 喜欢数
  visitors: number;      // 访问量
}
```

### User（用户）
```typescript
interface User {
  id: number;
  role: string;              // 角色
  account: string;           // 账号
  avatar: string;            // 头像
  password?: string;         // 密码（加密）
  nickname: string;          // 昵称
  mail: string;             // 邮箱
  phone: string;            // 电话
  registered_ip: string;    // 注册IP
  registered_time: Date;    // 注册时间
  status: number;           // 状态
}
```

## 🚧 后续改进建议

### 功能增强
1. 添加评论系统
2. 添加文章搜索功能
3. 添加文章分类功能
4. 添加富文本编辑器
5. 添加图片上传功能
6. 添加用户权限管理
7. 添加文章草稿功能

### 性能优化
1. 实现图片懒加载
2. 添加 Redis 缓存
3. 实现 CDN 加速
4. 优化首屏加载速度

### 用户体验
1. 添加加载动画
2. 添加错误提示
3. 添加骨架屏
4. 优化移动端体验

## 💡 常见问题

### Q: 如何修改默认管理员密码？
A: 编辑 `src/lib/db.ts` 中的 `mockUsers` 数组，使用 bcrypt 生成新的密码哈希。

### Q: 如何添加新的文章？
A: 登录后访问 `/admin/create`，填写表单并提交。

### Q: 如何自定义主题颜色？
A: 修改 Tailwind 配置文件或使用 Ant Design 的主题定制功能。

### Q: 如何部署到生产环境？
A: 
1. 构建项目：`pnpm build`
2. 启动服务：`pnpm start`
3. 推荐使用 Vercel、Netlify 等平台部署

## 📞 技术支持

如有问题，请参考：
- [Next.js 文档](https://nextjs.org/docs)
- [React 文档](https://react.dev)
- [Ant Design 文档](https://ant.design)
- [Tailwind CSS 文档](https://tailwindcss.com)

---

**项目完成时间**: 2025-10-30
**技术栈**: Next.js 15 + React 19 + Ant Design 6 + Tailwind CSS 4 + TypeScript

