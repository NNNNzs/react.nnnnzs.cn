# ==================== 数据库配置 ====================
# MySQL 数据库配置 (Prisma 使用 DATABASE_URL，迁移脚本使用 DB_* 变量)
# 注意：DATABASE_URL 中的数据库名应与 DB_DATABASE 保持一致
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=your_mysql_password
DB_DATABASE=blog
DATABASE_URL="mysql://root:your_mysql_password@localhost:3306/blog"

# ==================== Redis 配置 ====================
# Redis 连接配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# ==================== 应用配置 ====================
# Node 环境 (development/production)
NODE_ENV=development
PORT=3000

# JWT Secret (用于加密token)
JWT_SECRET=your-secret-key-here-change-in-production

# API Base URL (可选)
NEXT_PUBLIC_API_URL=

# ==================== 腾讯云 COS 配置 ====================
# 用于文件上传功能（Markdown 编辑器中的图片上传）
SecretId=your-cos-secret-id
SecretKey=your-cos-secret-key
Bucket=your-bucket-name
Region=ap-shanghai
CDN_URL=https://static.your-domain.com

# ==================== Docker 部署配置 ====================
# Docker 容器中连接宿主机服务时使用
# Mac/Windows: host.docker.internal
# Linux: 使用宿主机的实际 IP 地址
DOCKER_HUB_TOKEN=your-docker-hub-token
DOCKERHUB_USERNAME=your-dockerhub-username

# ==================== 向量数据库 ====================
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=your-qdrant-api-key

# ==================== 微信小程序 ====================
WX_APP_ID=your-wechat-app-id
WX_APP_SECRET=your-wechat-app-secret

# ==================== GitHub OAuth ====================
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost:3000/api/github/callback

NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ==================== 说明 ====================
# 1. 数据库配置：
#    - DB_* 变量用于迁移脚本
#    - DATABASE_URL 用于 Prisma ORM
#    - 两者数据库名必须保持一致
#
# 2. Docker 部署：
#    - 在 Docker 容器中，将 DB_HOST 和 REDIS_HOST 改为 host.docker.internal
#    - 或者使用 Docker Compose 管理服务
#
# 3. AI 配置（已迁移至数据库）：
#    - AI 模型配置（chat, embedding, description, ai_text）已从环境变量迁移到数据库
#    - 请在系统配置管理页面添加相关配置，或使用 scripts/batch-add-ai-configs.js 批量添加
#    - 参考 scripts/README-AI-CONFIG.md 了解详细配置方法
#
# 4. 安全提示：
#    - 生产环境中务必修改所有默认密码和密钥
#    - 不要将包含真实密钥的 .env 文件提交到 Git
